import type { ReactNode } from "react"

type ContactProps={};

 const Contact = ({}:ContactProps):ReactNode => {
  return (
    <div className="">Hello from Contact</div>
  );
}

export default Contact;
