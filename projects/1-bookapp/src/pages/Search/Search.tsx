import type { ReactNode } from "react"

type SearchProps={};

 const Search = ({}:SearchProps):ReactNode => {
  return (
    <div className="">Hello from Search</div>
  );
}

export default Search;
