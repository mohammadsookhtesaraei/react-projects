import type { ReactNode } from "react"

type CartPageProps={};

 const CartPage = ({}:CartPageProps):ReactNode => {
  return (
    <div className="">Hello from CartPage</div>
  );
}

export default CartPage;
