import type { ReactNode } from "react"

type StatsProps={};

 const Stats = ({}:StatsProps):ReactNode => {
  return (
    <div className="">Hello from Stats</div>
  );
}

export default Stats;
