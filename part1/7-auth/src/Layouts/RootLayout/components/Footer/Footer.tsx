import type { ReactNode } from "react"

type FooterProps={};

 const Footer = ({}:FooterProps):ReactNode => {
  return (
    <div className="">Hello from Footer</div>
  );
}

export default Footer;
