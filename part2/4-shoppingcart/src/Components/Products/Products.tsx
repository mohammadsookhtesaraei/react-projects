import type { ReactNode } from "react"

type ProductsProps={};

 const Products = ({}:ProductsProps):ReactNode => {
  return (
    <div className="">Hello from Products</div>
  );
}

export default Products;
