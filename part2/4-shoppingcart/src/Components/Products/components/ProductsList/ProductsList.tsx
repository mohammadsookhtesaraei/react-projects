import type { ReactNode } from "react"

type ProductsListProps={};

 const ProductsList = ({}:ProductsListProps):ReactNode => {
  return (
    <div className="">Hello from ProductsList</div>
  );
}

export default ProductsList;
