import type { ReactNode } from "react"

type ProductCardProps={};

 const ProductCard = ({}:ProductCardProps):ReactNode => {
  return (
    <div className="">Hello from ProductCard</div>
  );
}

export default ProductCard;
