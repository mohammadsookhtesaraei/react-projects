import type { ReactNode } from "react"

type CountProps={};

 const Count = ({}:CountProps):ReactNode => {
  return (
    <div className="">Hello from Count</div>
  );
}

export default Count;
